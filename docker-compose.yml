version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./rag_storage:/app/rag_storage
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: always
